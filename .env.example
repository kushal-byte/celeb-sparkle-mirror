# Copy this file to .env.local and fill in the values from your Supabase project
# After creating .env.local restart the dev server (npm run dev)

# URL for your Supabase project (example: https://abcd1234.supabase.co)
VITE_SUPABASE_URL=your key 
# Public anon key for your Supabase project (found under Settings → API → Project API keys)
VITE_SUPABASE_ANON_KEY=ur key
# Optional: if you deploy Supabase Edge Functions, they will be available under:
# ${VITE_SUPABASE_URL}/functions/v1/<function-name>
# If you don't deploy functions, the app falls back to the aiService in some flows.
VITE_OPENROUTER_API_KEY=ur api keys # not recommended for client — only for function
# Optional local overrides (not required for hosted Supabase):
# VITE_SUPABASE_URL=http://localhost:54321
# VITE_SUPABASE_ANON_KEY=public-anon-key
